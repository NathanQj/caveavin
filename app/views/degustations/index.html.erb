<%= link_to "Ajouter une dégustation", new_degustation_path, class: "btn btn-primary" %>
<% @degustations.each do |degustation| %>
  <%= degustation.product.name %>
  <%= degustation.product.year %>
<% end %>
